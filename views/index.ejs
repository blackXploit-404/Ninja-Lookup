<!DOCTYPE html>
<html lang="en">
<head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-PR9BHV5V50"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-PR9BHV5V50');
</script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lookup-Now 🚀</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #1a1a1a;
      margin: 0;
      padding: 20px;
      color: white;
    }
    .container {
      max-width: 800px;
      margin: auto;
      padding: 20px;
      background: #2c2c2c;
      border-radius: 10px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    }
    h1 {
      text-align: center;
      color: #ffffff;
      animation: fadeIn 0.5s;
    }
    h2 {
      text-align: center;
      color: #b3b3b3;
      margin-bottom: 30px;
      animation: fadeIn 0.5s;
    }
    form {
      display: flex;
      justify-content: center;
      margin-bottom: 20px;
      animation: slideIn 0.5s;
    }
    input[type="text"] {
      padding: 12px;
      border: 1px solid #555;
      border-radius: 5px;
      width: 70%;
      margin-right: 10px;
      transition: border-color 0.3s;
      background-color: #1a1a1a;
      color: white;
    }
    input[type="text"]:focus {
      border-color: #28a745;
      outline: none;
    }
    button {
      padding: 12px 20px;
      border: none;
      border-radius: 5px;
      background-color: #28a745;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
      margin-left: 10px;
    }
    button:hover {
      background-color: #218838;
      transform: scale(1.05);
    }
    #result-area, #whois-area {
      margin-top: 20px;
      border: 1px solid #ccc;
      padding: 15px;
      border-radius: 5px;
      background-color: #f9f9f9;
      color: black;
      animation: fadeIn 0.5s;
    }
    #res-i-map {
      margin-top: 15px;
    }
    footer {
      text-align: center;
      margin-top: 20px;
      font-size: 0.9em;
      color: #666;
    }
    img {
      margin-top: 10px;
      border-radius: 5px;
    }
    .loader {
      display: none;
      border: 4px solid #f3f3f3;
      border-top: 4px solid #3498db;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: auto;
    }
    .error-message {
      color: #ff6b6b;
      margin-top: 10px;
      text-align: center;
      font-weight: bold;
    }
    .hidden {
      display: none;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    @keyframes slideIn {
      from { transform: translateY(-20px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Lookup Now 🔍</h1>
    <h2>Explore Domains, Discover Hosting—All in One Click! 🚀</h2>
    <form id="lookup-form">
      <input type="text" id="domain_input" placeholder="Enter domain name..." required>
      <button type="button" id="domain_lookup_btn">Domain Lookup</button>
      <button type="button" id="hosting_lookup_btn">Hosting Lookup</button>
    </form>
    <p id="error-message" class="error-message hidden">Please enter a domain name.</p>

    <div class="loader" id="loader"></div>

    <div id="whois-area" style="display: none;">
      <h3>WHOIS Information 📄</h3>
      <pre id="res-whois-json" style="white-space: pre-wrap;"></pre>
      <div style="text-align: center; margin-top: 10px;">
        <p style="color: #666;">Powered by WHOIS</p>
        <img src="https://www.whois.com/images/logo.svg" alt="WHOIS Logo" style="height: 30px; margin-top: 5px;">
      </div>
    </div>

    <div id="result-area" style="display: none;">
      <h3 id="hosting-message"></h3>
      <div id="hosting-image"></div>
      <p><strong>IP:</strong> <span id="res-i-ip"></span></p>
      <p><strong>IP Type:</strong> <span id="res-i-iptype"></span></p>
      <p><strong>ISP:</strong> <span id="res-i-isp"></span></p>
      <p><strong>City:</strong> <span id="res-i-city"></span></p>
      <p><strong>Region:</strong> <span id="res-i-region"></span></p>
      <p><strong>Country:</strong> <span id="res-i-country"></span></p>
      <p><strong>Longitude:</strong> <span id="res-i-longitude"></span></p>
      <p><strong>Latitude:</strong> <span id="res-i-latitude"></span></p>
      <div id="res-i-map"></div>
    </div>

    <footer>
      <p>&copy; 2024 Lookup-Now 🚀. All rights reserved.</p>
      <p>A Devsecure Innovations Product</p>
      <p><a href="https://github.com/blackXploit-404/Lookup-Now</a>Check On GitHub ⭐</p>
    </footer>
  </div>

  <script>
    function showError(message) {
      $("#error-message").text(message).removeClass("hidden");
    }

    function hideError() {
      $("#error-message").addClass("hidden");
    }

    $("#domain_lookup_btn").click(function() {
      const domain = $("#domain_input").val();
      if (domain === "") {
        showError("Please enter a domain name.");
        return;
      }
      hideError();
      fetch_whois(domain);
    });

    $("#hosting_lookup_btn").click(function() {
      const domain = $("#domain_input").val();
      if (domain === "") {
        showError("Please enter a domain name.");
        return;
      }
      hideError();
      hosting_lookup(domain);
    });

    function showLoader() {
      $("#loader").show();
      $("#result-area").hide();
      $("#whois-area").hide();
    }

    function hideLoader() {
      $("#loader").hide();
    }

    function fetch_whois(domain) {
      showLoader();
      $.get(`/whois?domain=${domain}`, function(data) {
        hideLoader();
        $("#res-whois-json").text(JSON.stringify(data, null, 2));
        $("#whois-area").show();
      }).fail(function() {
        hideLoader();
        alert('Error fetching WHOIS data');
        $("#whois-area").hide();
      });
    }

    function hosting_lookup(domain) {
      showLoader();
      $.ajax({
        type: "GET",
        url: `https://ipwhois.app/json/${domain}`,
        success: function(data) {
          hideLoader();
          const hostingProvider = data.isp || 'N/A';
          $("#hosting-message").text(`${domain} is hosted on ${hostingProvider} 🎉`);

          const hostingImageMap = {
            "Google LLC": "https://www.google.com/images/branding/googlelogo/2x/googlelogo_color_92x30dp.png",
            "Microsoft Corporation": "https://upload.wikimedia.org/wikipedia/commons/4/44/Microsoft_logo.svg"
            // Add more mappings as necessary
          };

          if (hostingImageMap[hostingProvider]) {
            $("#hosting-image").html(`<img src="${hostingImageMap[hostingProvider]}" style="width: 150px;">`);
          } else {
            $("#hosting-image").html('');
          }

          $("#res-i-ip").text(data.ip || 'N/A');
          $("#res-i-iptype").text(data.type || 'N/A');
          $("#res-i-isp").text(data.isp || 'N/A');
          $("#res-i-city").text(data.city || 'N/A');
          $("#res-i-region").text(data.region || 'N/A');
          $("#res-i-country").text(data.country || 'N/A');
          $("#res-i-longitude").text(data.longitude || 'N/A');
          $("#res-i-latitude").text(data.latitude || 'N/A');
          $("#result-area").show();
        }
      });
    }
  </script>
</body>
</html>
